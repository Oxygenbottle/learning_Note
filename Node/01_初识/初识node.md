> `思考：` 为什么JavaScript能被浏览器执行
> `解答：` 因为浏览器中有JavaScript的解析引擎，并且不同的浏览器使用的引擎也是不同的。
>  
> 例如：
> chrome => V8
> FireFox => OdinMonkey
> safari => JSCore
> IE => Chakra
> ...

##### 什么是 Node.js

`Node.js是` 一个基于Chrome V8引擎的 `JavaScript运行环境`

`cmd` 是旧版本的终端，由微软升级城的 `PowerShell`
